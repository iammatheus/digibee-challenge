import { DrawerBody } from '../DrawerBody'
import { DrawerBodyProps } from '@heroui/react'

export function StepDrawerBody({ children }: DrawerBodyProps) {
  return <DrawerBody>{children}</DrawerBody>
}
